{
    "name": "dsjodin/hostdeployer",
    "description": "Automatiserad ESXi-provisionering: DHCP -> iPXE -> HTTP -> kickstart",
    "type": "project",
    "license": "proprietary",
    "require": {
        "php": ">=8.1"
    },
    "require-dev": {
        "phpunit/phpunit": "^11.0",
        "phpstan/phpstan": "^2.0"
    },
    "scripts": {
        "test": "phpunit",
        "lint": "phpstan analyse",
        "syntax": "find lib www scripts tests -name '*.php' -print0 | xargs -0 -n1 php -l"
    },
    "config": {
        "sort-packages": true
    }
}
