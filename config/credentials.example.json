{
    "_comment": "Copy to credentials.json (mode 0640, owned by the web server user) and fill in real secrets. Never commit the live file.",
    "ilo": {
        "admin_user": "Administrator",
        "admin_password": "CHANGEME",
        "hosts": {}
    },
    "esxi": {
        "root_password": "CHANGEME",
        "hosts": {}
    }
}
